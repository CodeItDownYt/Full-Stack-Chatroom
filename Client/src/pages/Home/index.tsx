import RoomNavbar from "../../components/RoomNavbar";

const Home = () => {
	return (
		<div className="w-full h-full relative">
			<RoomNavbar roomTitle="Select a Room To Join" />
		</div>
	);
};

export default Home;
